import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    flex: 1;
    justify-content: flex-end;
    align-self: flex-end;

    img {
        max-width: 650px;
        max-height: 650px;
        width: auto;
        height: auto;
    }
`